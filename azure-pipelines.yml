# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: mvn -ntp package
  displayName: 'build verademo'

- script: |
      export SRCCLR_SCM_NAME=VeraDemoJava
      export SRCCLR_SCM_URI=https://dev.azure.com/cpollock/VeraDemoJava
      export SRCCLR_SCM_REF=master
      export SRCCLR_SCM_REF_TYPE=branch
      export SRCCLR_SCM_REV=123456
      export SRCCLR_API_TOKEN="$(SRCCLR_API_TOKEN)"
      curl -sSL https://download.sourceclear.com/ci.sh | bash -s -- scan --skip-compile --update-advisor
  displayName: 'Veracode SCA scan'
 
